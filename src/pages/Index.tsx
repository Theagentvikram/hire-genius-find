
import { Layout } from "@/components/Layout";
import { Landing } from "@/components/Landing";

const Index = () => {
  return (
    <Layout>
      <Landing />
    </Layout>
  );
};

export default Index;
